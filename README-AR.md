# دليل التشغيل السريع - INZO LLC Report System

## المشكلة: البرنامج لا يعمل عند صديقك؟

السبب الرئيسي: **MongoDB غير مثبت أو غير شغال**

---

## ✅ الحل الأسهل والأسرع (خطوتين فقط):

### الخطوة 1: تشغيل MongoDB
**قبل أي شيء، يجب تشغيل MongoDB:**

1. افتح ملف: **`start-mongodb.bat`** (كليك يمين → Run as Administrator)
2. سيظهر window أصفر - **اتركه مفتوح ولا تقفله!**

### الخطوة 2: تشغيل السيرفر
1. افتح ملف: **`start-server.bat`**
2. سيفتح البرنامج في المتصفح تلقائياً ✓

---

## 🔧 إذا لم يعمل: تثبيت MongoDB

### لو ظهرت رسالة خطأ في `start-mongodb.bat`:

**يعني MongoDB غير مثبت. اتبع هذه الخطوات:**

1. **تحميل MongoDB:**
   - اذهب إلى: https://www.mongodb.com/try/download/community
   - اختر: **Windows** و **MSI**
   - حمّل الملف

2. **تثبيت MongoDB:**
   - شغّل ملف الـ MSI
   - أثناء التثبيت، **تأكد من تفعيل**:
     ✅ **"Install MongoDB as a Service"**
   - اضغط Next حتى ينتهي التثبيت

3. **تشغيل البرنامج:**
   - بعد التثبيت، شغّل `start-server.bat`
   - المفروض يشتغل مباشرة!

---

## 📋 الطريقة البديلة (يدوي):

إذا كنت تفهم في Command Prompt:

1. افتح **Command Prompt كـ Administrator**
2. اكتب:
   ```
   mkdir C:\data\db
   mongod --dbpath C:\data\db
   ```
3. اترك هذا الـ window مفتوح
4. افتح window جديد وشغّل `start-server.bat`

---

## ⚠️ ملاحظات مهمة:

- ✅ **Node.js** يجب أن يكون مثبت (يتم فحصه تلقائياً)
- ✅ **MongoDB** يجب أن يكون شغال قبل تشغيل السيرفر
- ✅ احتفظ بـ window الـ MongoDB مفتوح طول ما البرنامج شغال
- ⚠️ لا تقفل window السيرفر الأسود، فقط صغّره (Minimize)

---

## 📞 الأخطاء الشائعة:

### ❌ "MongoDB connection error"
**الحل:** MongoDB غير شغال. شغّل `start-mongodb.bat` أولاً.

### ❌ الـ window يقفل مباشرة
**الحل:** Node.js غير مثبت. حمّله من https://nodejs.org/

### ❌ "Cannot find module"
**الحل:** احذف مجلد `backend\node_modules` وشغّل `setup.bat`

---

## 🎯 الترتيب الصحيح:

```
1. start-mongodb.bat  (اتركه مفتوح)
   ↓
2. start-server.bat   (شغّله في window جديد)
   ↓
3. البرنامج يفتح في المتصفح ✓
```

---

## 💡 نصيحة:

لو تعبت من تشغيل MongoDB يدوي كل مرة:
- ثبّت MongoDB **as a Service** (هيشتغل تلقائياً مع الويندوز)
- بعدها مش هتحتاج تشغل `start-mongodb.bat`
- هيكفي بس `start-server.bat`

---

## 🔄 تحسين موثوقية التليجرام

إذا كان السيرفر يواجه تأخيرات في الاتصال بتليجرام (مثل `ETIMEDOUT`)، النظام الآن يحاول الإرسال مجدداً تلقائياً مع تأخير متزايد.
يمكن التحكم بالخيارات التالية عبر `backend/config.json` أو متغيرات البيئة لـ `backend`:
- `TELEGRAM_MAX_ATTEMPTS` (قيمة افتراضية: `3`)
- `TELEGRAM_INITIAL_DELAY_MS` (افتراضي: `1200` مللي ثانية)
- `TELEGRAM_MAX_DELAY_MS` (افتراضي: `5000` مللي ثانية)
- `TELEGRAM_BACKOFF_FACTOR` (افتراضي: `1.75`)

---

بالتوفيق! 🚀
